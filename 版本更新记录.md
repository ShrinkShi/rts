# Iron Meridian RTS 版本更新记录

> 项目：GodotRTS / Iron Meridian RTS  
> 引擎：Godot 4.7+，GDScript  
> 文档整理日期：2026-07-27  
> 记录依据：现有项目历史对话、已知补丁说明与 GitHub `ShrinkShi/rts` 当前公开基线。  
> 说明：没有可靠原始记录的版本不会补写不存在的功能，而是明确标记“资料缺失”。

---

## 版本状态说明

- **已发布**：存在明确工程包或补丁说明。
- **热修复**：针对特定已发布版本的错误修复。
- **已废弃**：方案被后续架构替代，不应继续扩展。
- **资料缺失**：确认版本存在，但现有材料不足以还原完整改动。
- **规划中**：尚未实现，不得视为已完成功能。

---

## 早期版本

### v0.1.x

**状态：资料缺失**

当前材料没有足够信息确认具体版本号、发布日期和功能清单。

### v0.2.0

**状态：已发布，资料不完整**

已知信息：

- 形成可由 Godot 4.7/4.7.1 导入并运行的完整 RTS 工程。
- 作为后续 v0.3.0 的基础版本。
- 具体逐项更新内容在当前材料中缺失。

### v0.3.0

**状态：已发布，资料不完整**

已知信息：

- 基于 v0.2.0 完整工程继续修改，并非重新生成或回退功能。
- 具体逐项更新内容在当前材料中缺失。

### v0.4.x - v0.7.x

**状态：资料缺失**

当前材料没有可靠的独立版本说明，不做推测性补写。

### v0.8.x

**状态：已发布**

已知基线：

- v0.8 系列已知公开基线版本为 **v0.8.4**。
- 项目名称为 **Iron Meridian RTS**。
- 已存在核心自动加载模块：
  - `GameConfig`
  - `EventBus`
  - `SaveManager`
  - `CursorManager`
  - `VoiceManager`
  - `NetworkManager`
- 鼠标光标仍为程序化绘制版本。
- 该条仅描述 v0.8.4 当时状态；仓库现已同步至 v0.15.0。

---

## RA2/YR 素材兼容阶段

### v0.9.2

**状态：稳定基线**

已知定位：

- 被选为 RA2/YR 兼容层重构时的稳定 RTS 底座。
- 后续应在该版本之上重做资源索引、INI 合并、Art 动画解析、调色板渲染和资源验证，而不是继续修补错误的数据模型。
- 当前材料没有独立安装包说明和完整改动列表。

### v0.9.3

**状态：已发布**

#### RA2/YR 素材兼容层

新增 SHP(TS) 支持：

- 解析原始帧。
- 解析按行 RLE 压缩帧。
- 导出透明 PNG 图集。
- 生成 `SpriteFrames.tres`。
- 生成可按帧访问的 `RA2SHPResource`。
- 支持通过 JSON 描述 `stand_0`、`move_0`、`attack_0`、`death_0` 等动画。
- 明确 SHP 为调色板索引图像，必须搭配正确 PAL 文件。

新增 VXL + HVA 支持：

- 解析体素几何。
- 解析颜色索引与法线索引。
- 读取 HVA 部件矩阵与动画帧。
- 支持车体、炮塔、炮管分组件导出。
- 默认导出八方向透明序列帧。
- 支持配置更多方向。

新增 WAV 支持：

- WAV 自动复制到 Godot 资源目录。
- 使用 Godot 原生音频导入器处理。
- 增加 `RA2AssetLibrary.load_wav()` 入口。

#### 首批接入素材

- `ggi.shp`：步枪兵/重装大兵测试 Prefab。
- `htk.vxl + htk.hva`：坦克车体。
- `htktur.vxl + htktur.hva`：独立炮塔。
- `htkbarl.vxl + htkbarl.hva`：炮管。
- `ygpowr.shp`：发电站建筑。

#### 工具与文档

- 新增 RA2 素材预览场景。
- 新增 Godot 编辑器右侧 RA2 素材导入面板。
- 新增 Windows 批处理导入入口。
- 新增 `docs/RA2_ASSET_PIPELINE.md`。

#### 已知限制

- 当时缺少正确 SHP 调色板，部分素材使用回退 PAL，颜色不准确。
- VXL 使用软件等距投影近似渲染。
- 尚未接入完整 VPL 与法线着色表。
- 未完成 Godot 4.7 F5 实机运行验证。

### v0.10.0

**状态：已废弃**

该阶段采用了后续证明不可维护的方案：

- 为大量对象分别生成高度重复的 `.tscn`。
- 自动猜测 SHP 动画分段。
- 把 VXL 简化为八方向截图。
- 把武器系统压扁为通用 `damage/range/cooldown`。
- 使用统一调色板回退。
- 根据文件名猜测主体、炮塔和炮管。

### v0.10.1

**状态：已废弃**

延续 v0.10.0 的错误架构，不应继续扩展。

后续确定的新方向：

- 使用少量通用运行时场景：
  - `ra2_infantry.tscn`
  - `ra2_shp_vehicle.tscn`
  - `ra2_voxel_vehicle.tscn`
  - `ra2_aircraft.tscn`
  - `ra2_building.tscn`
  - `ra2_defense_building.tscn`
  - `ra2_animation.tscn`
- 单位差异来自结构化数据。
- 数据字段必须保留来源文件、节和键。
- Rules → Art → 素材引用链必须完整保留。
- 步兵 Sequence、SHP 载具分段、VXL 组件、建筑附属动画、剧院资源、所属色和武器引用链必须分别解析。

### v0.11.x - v0.12.x

**状态：资料缺失**

当前材料无法可靠恢复这些版本的独立更新内容。

---

## 数据库与预览修复阶段

### v0.13.0

**状态：已发布，资料不完整**

已知状态：

- 已形成 RA2 数据库与资源预览系统。
- 已开始批量解析单位、建筑、动画和音效。
- 存在 Parser Hotfix 1。
- 后续 v0.13.1 以“v0.13.0 + Parser Hotfix 1”为安装基础。
- 当时仍存在方向错误、建筑组件错误、剧院资源匹配错误和旧预览残留。

### v0.13.0 Parser Hotfix 1

**状态：热修复，资料不完整**

确认存在，但现有材料没有保留完整错误信息和修改文件清单。

### v0.13.1

**状态：已发布**

#### SHP 方向修复

确认 RA2 步兵与 SHP 载具原始方向顺序，并映射到项目方向系统。

修复：

- 步兵南北方向颠倒。
- 东南、西南、东北、西北方向颠倒。
- 恐怖机器人等 SHP 载具方向颠倒。
- VXL/HVA 载具使用独立方向投影，不改动原本正确的 VXL 方向。

#### 建筑组合动画

新增：

- `Operational`
- `DamagedOperational`

`Operational` 合成：

- 主体 SHP。
- `ActiveAnim`
- `ActiveAnimTwo`
- `ActiveAnimThree`
- `ActiveAnimFour`
- `IdleAnim`
- `IdleAnimTwo`
- 各组件 X/Y 偏移。
- `ZAdjust`
- `YSort`
- 供电状态字段。

`DamagedOperational` 使用受损主体和对应 `*Damaged` 组件。

修复整栋建筑画面型 SHP 互相覆盖问题：

- 不再直接叠加完整图。
- 提取动态变化区域后再合成。

#### 剧院资源修复

修正 `NewTheater=yes` 文件名规则：

- T：温带。
- A：雪地。
- U：城市。
- D：沙漠。
- L：月球。
- N：新城市。
- G：通用回退。

查找顺序：

1. 当前剧院专用素材。
2. G 通用素材。
3. 原始注册名。

修复界面显示温带但实际加载雪地素材的问题。

#### 清理脚本

新增 `apply_v0.13.1_cleanup.bat`：

- 删除 7,490 个废弃预览文件。
- 删除 `.godot` 导入缓存。
- 保留仍在使用的预览、音频和数据库。
- 清理约 90 MB 旧资源。

#### 包结构

- 核心补丁。
- 预览资源 1/3。
- 预览资源 2/3。
- 预览资源 3/3。
- 合计约 98 MB。

### v0.13.2

**状态：已发布，资料不完整**

已知信息：

- 是 v0.14.0 增量补丁的直接基础版本。
- 完成了 v0.13.1 后续建筑组件修复与预览生成。
- 当前材料没有独立版本说明，不能可靠拆分其全部改动。

已知相关修复方向：

- `BibShape` 进入建筑下层合成。
- `SpecialAnim` - `SpecialAnimFour` 进入组件索引。
- `SuperLowPower` 与受损状态进入组件索引。
- 正常/受损共用 SHP 时拆分帧库。
- 支持建筑 VXL 炮塔与炮管。
- 盖特机炮改为“建筑 SHP 主体 + VXL 炮塔”。
- 武装直升机增加 Deploy 预览。
- 增量生成器按 ID 重建时不再清空全部预览。

---

## 真实素材接入实际游戏阶段

### v0.14.0

**状态：已发布**

增量包大小约 2.22 MB，基础版本为 v0.13.2。

#### 名称与 CSF

- 不再直接显示 `Name:CAR` 等标签。
- 使用 CSF 不区分大小写解析中文名称。
- 搜索框同时索引：
  - 对象 ID。
  - CSF 标签。
  - 中文名称。

示例：

- `CAR` → 汽车。
- `PIGCAR` → 警车。
- `TAXI` → 计程车。
- `CRUISE` → 游轮。

#### 海洋单位方向

- 修复海豚 `DLPH` 东西方向和斜向方向。
- 修复巨型乌贼 `SQD` 东西方向和斜向方向。
- 新增海洋单位专用 SHP 方向映射。

#### 盖特坦克状态

- `Stand`：显示炮塔，但不播放开火 HVA。
- `Fire`：播放完整炮塔旋转与开火 HVA。
- 停止攻击后恢复 `Stand`。

#### RA2/YR 视觉接入实际遭遇战

将资源管线接入：

- `unit.gd`
- `building.gd`
- 生产栏。
- 选中头像。
- 音频系统。

阵营显示名称：

- `union` → 盟军。
- `dominion` → 苏军。
- `republic` → 尤里。

基础单位映射：

盟军：

- 美国大兵 `E1`
- 重装大兵 `GGI`
- 灰熊坦克 `MTNK`
- 多功能步兵车 `FV`
- 超时空采矿车 `CMIN`

苏军：

- 动员兵 `E2`
- 防空步兵 `FLAKT`
- 犀牛坦克 `HTNK`
- 防空履带车 `HTK`
- 武装采矿车 `HARV`

尤里：

- 尤里新兵 `INIT`
- 病毒狙击手 `VIRUS`
- 狂风坦克 `LTNK`
- 盖特坦克 `YTNK`
- 奴隶矿场载具 `SMIN`

基础建筑映射：

- 建造厂。
- 发电设施。
- 兵营。
- 矿石精炼厂。
- 战车工厂。
- 维修厂或部队回收站。
- 基础防御塔。
- 碉堡。

#### 游戏内动画状态

步兵：

- 待机：`Ready / Guard / Idle`
- 移动：`Walk`
- 攻击：`FireUp`
- 死亡：`Die1 / Die2`
- 死亡动画强制单次播放。

载具：

- 停止：`Stand`
- 移动：按移动方向切换八方向 `Stand`
- 攻击：`Fire`，缺失时回退 `Stand`

建筑：

- 建造：`Buildup`
- 正常：`Operational / Ready`
- 受损：`DamagedOperational / DamagedReady`
- 摧毁：`Rubble / Damaged Body`
- 生产：`ProductionAnim / DeployingAnim`
- 矿车卸矿：`SpecialAnim`
- 维修：`SpecialAnim`
- 停电：静态主体回退

#### 所属色与音频

所属色改为双层图像：

- 原始基础图层。
- 所属色 remap 遮罩。

不再整体染色。

接入：

- `VoiceSelect`
- `VoiceMove`
- `VoiceAttack`
- `Weapon Report`
- `DieSound`
- 建筑/防御武器 Report

增加同类语音冷却，避免框选大量单位时叠加播放。

#### UI

- 生产栏显示对应 RA2/YR 素材首帧。
- 叠加所属色。
- 显示 CSF 中文名称。
- 鼠标提示显示中文名称。
- 底部选中头像使用真实素材。

#### 当时边界

- 仅把三方现有可玩槽位替换为 RA2/YR 视觉、动画、所属色和声音。
- 没有把数据库全部对象加入科技树与生产栏。
- 战斗数值、寻路、科技树和生产框架仍沿用 Iron Meridian 既有规则。

---

## 2.5D 战斗与 AI 阶段

### v0.15.0

**状态：已发布**

增量包约 3.8 MB。  
基础版本：v0.14.0。  
SHA-256：

`b184e677ad0251b9f761224fec436381e244113b449e18cc9efd4dda806d10ec`

#### RA2/YR 副官语音

三方副官：

- 盟军：`CEVA`
- 苏军：`CSOF`
- 尤里：`CYUR`

替换事件：

- 建筑建造完成。
- 单位生产完成。
- 建造中。
- 资金不足。
- 电力不足。
- 暂停与取消。
- 维修。
- 基地遭到攻击。
- 单位遭到攻击。
- 矿车遭到攻击。
- 无法部署。

原版没有对应录音的自定义事件才回退系统 TTS。

#### 车身与炮塔分层

已接入单位：

- 灰熊坦克。
- 多功能步兵车。
- 犀牛坦克。
- 防空履带车。
- 狂风坦克。
- 盖特坦克。
- 武装采矿车。
- 奴隶矿场载具。

实现：

- 车身朝移动方向转动。
- 炮塔独立朝目标转动。
- 移动中持续瞄准和开火。
- 炮塔以八方向、45° 步进显示。
- 盖特坦克正常与开火 HVA 分离。
- 减少炮塔旋转时因投影边界变化导致的抖动。

#### RA2 温带地形过渡管线

新增 TMP 解析并接入：

- 草地。
- 泥地与道路地表。
- 水面。
- 岩地。
- 矿区地表。

素材来源：

- `isotemp`
- `isotem.pal`

限制：

- 底层仍是 32×32 矩形逻辑网格。
- 只是把 RA2 TMP 像素适配到旧网格。
- 尚未重构成 RA2 等距坐标。
- 尚未加载原版 `.map` 的：
  - `IsoMapPack5`
  - 桥梁。
  - 悬崖。
  - 坡面。
  - 高度层。

#### 美国大兵部署

按 `D`：

- `Deploy`
- `Deployed`
- `DeployedFire`

部署后：

- 射程提高 38%。
- 装填时间降低 42%。
- 伤害提高 15%。
- 原地自动攻击。

下达移动、巡逻或攻击移动命令时解除部署。

#### 坦克炮弹

- 取消瞬时点对点伤害。
- 新增独立炮弹实体。
- 存在实际飞行时间。
- 使用可见抛物线。
- 抵达目标后结算伤害。
- 支持直击和范围爆炸。
- 目标移动时追踪其当前位置。

防御射程调整：

- 机枪碉堡。
- 哨戒炮。
- 盖特机炮。

三者射程与主战坦克统一。

#### 电脑 AI 建造与经济

玩家和电脑开局：

- 190 资金。
- 建造厂。
- 一座发电设施。
- 两名基础步兵。

AI 建造顺序：

1. 兵营。
2. 矿石精炼厂。
3. 战车工厂。
4. 第二座发电设施。
5. 维修设施。
6. 防御塔。
7. 碉堡。

实现：

- 实际扣除建造资金。
- 建筑建造时间。
- 建筑放置。
- 前置建筑判断。
- 精炼厂赠送矿车。
- 矿车采矿发展经济。
- 为关键建筑预留资金。
- 矿车全灭后的有限防锁死补助。
- 保留部分守军。
- 发展完成后组织进攻。
- 进攻途中优先反击。

#### 调试开关

- `F8`：AI 调试。
- `F9`：全图视野。

AI 调试显示：

- 剩余资金。
- 当前建造建筑。
- 建造进度。
- 当前生产单位。
- 生产进度。
- 当前发展计划。
- 可用进攻军力。

#### 已知验证边界

已完成静态与资源检查，但当时没有 Godot 4.7.1 可执行文件，未完成真实 F5 对局验证。

### v0.15.0 Parser Hotfix 1

**状态：热修复**

修复文件：

- `scripts/game/unit.gd`

问题：

- 旧代码使用三元表达式在 `RA2VisualPlayer` 与 `RA2LayeredVehicleVisual` 之间创建对象。
- Godot 静态推断把局部变量推断为普通 `RA2VisualPlayer`。
- 分层载具调用四参数 `play_state()` 时触发 Parser Error。

修复：

- 将两种视觉创建逻辑拆为明确分支。
- 普通视觉调用：
  - `play_state(state, direction, restart)`
- 分层载具调用：
  - `play_state(state, body_direction, turret_direction, restart)`

安装后需要删除 `.godot/` 并重新导入。

---

## v0.16.0

**状态：开发中**

本版本目标来自 2026-07-27 新一轮优化要求。

### v0.16.0-dev.1（2026-07-27）

已完成：

- 修复矿石实体被 `z_index = -2` 放到地形后方的问题。
- 矿石实体改为使用现有 RA2 温带地形图集中的矿区帧，并保留缺失素材时的程序化回退。
- 矿石视觉随剩余储量缩小，枯竭后仅保留可选中状态，不再绘制假矿石。
- 解析用户提供的 `mouse.shp + mousepal.pal`，并将当前运行时使用的 63 个源帧打包为压缩调色板索引数据；原文件包含 450 帧。
- CursorManager 支持运行时重建 RA2 动画光标、逐状态帧率和独立热点。
- 解码失败时回退操作系统默认光标，并输出明确警告。
- 新增光标转换脚本、压缩索引数据文件与管线文档。

尚未完成：

- 真正把矿石从 TMP 地形类型拆成独立 Overlay 数据层。
- 八方向地图边缘滚动光标与战场边界状态联动。
- 原版等距高度地图、坡面、悬崖、水岸和桥梁。
- VXL 运行时 3D/2.5D 渲染及载具冲击波物理。
- Godot 4.7.1 本机 F5 对局验证。

后续目标如下：


### 1. 矿石实体恢复

目标：

- 区分“矿区地表”和“可采集矿石实体”。
- RA2 矿石按 Overlay 层处理，不再混入 TMP 地表层。
- 恢复矿石可见实体、储量、采集、枯竭与再生状态。
- 检查：
  - 素材引用。
  - 调色板。
  - `z_index` / YSort。
  - 地形层遮挡。
  - 资源节点是否被错误隐藏或释放。

### 2. RA2 风格光标

输入素材：

- `mouse.shp`

已确认该文件：

- SHP(TS) 格式。
- 画布尺寸 55×43。
- 共 450 帧。
- 光标帧使用无 RLE 的透明索引图像。

待完成：

- 已使用 `mousepal.pal` 正确着色并接入当前游戏正在调用的光标状态。
- 待把部署、进入、维修、出售、攻击移动和八方向边缘滚动状态接入相应命令模式。
- 已支持逐状态动画速率和热点位置。
- 解码失败时会明确警告并回退操作系统光标。

### 3. VXL 运行时 3D/2.5D 渲染

目标：

- 不再仅依赖预烘焙八方向截图。
- VXL 转换为运行时体素/网格数据。
- HVA 驱动车体、炮塔、炮管等节点变换。
- 使用正交相机进行 3D → 2D 等距投影。
- 保留 RA2 像素化观感。
- 支持：
  - 独立炮塔转向。
  - 车体倾斜。
  - 上下坡。
  - 高度变化。
  - 阴影投射。
  - 冲击波掀起。
  - 空中翻滚。
  - 落地碰撞与摧毁。

### 4. 等距高度地图

目标：

- 从 32×32 矩形过渡网格迁移到等距高度网格。
- 单元格数据至少包含：
  - X/Y。
  - TileIndex。
  - TileSubIndex。
  - Level。
  - Ramp/Slope。
  - LandType。
  - Passability。
  - Z 数据。
- 接入 `IsoMapPack5`。
- 支持坡面、悬崖、桥梁、水面和高度层。
- 寻路、射程、弹道、遮挡和建造判定统一使用高度数据。

### 5. RA2 地图对象与地形

目标：

- TMP：
  - 草地。
  - 道路。
  - 岩地。
  - 水体。
  - 海岸。
  - 坡面。
  - 悬崖。
- TerrainTypes：
  - 树木。
  - 岩石。
  - 场景装饰。
- OverlayTypes：
  - 矿石。
  - 宝石。
  - 墙体。
  - 其他覆盖物。
- SmudgeTypes：
  - 弹坑。
  - 焦痕。
- 所有对象使用对应剧院资源和调色板。

### v0.16.0 验收标准

- 矿石实体在实际游戏中可见、可采集、可枯竭。
- 光标状态与命令状态一致，动画和热点无偏移。
- 坦克上下坡时车身姿态正确。
- 炮塔在坡面和移动中仍能独立瞄准。
- 载具被冲击波掀起后具有高度、旋转和落地过程。
- 水体、坡面、悬崖和高度层影响通行与寻路。
- 树木、矿石和地形对象的遮挡顺序正确。
- 版本更新记录与代码提交同步更新。
- 必须通过 Godot 4.7.1 实际启动与对局验证，不再只做文本静态检查。

---

## 尚缺失的历史资料

以下版本没有足够可靠的独立说明：

- v0.1.x
- v0.2.0 详细变更
- v0.3.0 详细变更
- v0.4.x - v0.7.x
- v0.9.2 详细变更
- v0.11.x - v0.12.x
- v0.13.0 完整变更
- v0.13.0 Parser Hotfix 1 完整变更
- v0.13.2 完整变更

找到对应工程包、旧聊天导出或提交记录后，应补齐这些章节，禁止用猜测替代历史记录。
